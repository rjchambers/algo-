# Runtime dependencies, pinned (spec §3). Tested on Python 3.11.15.
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
requests==2.34.2
pydantic==2.13.4
pydantic-settings==2.14.1
structlog==26.1.0
ccxt==4.5.56
# Phase 3 (trading/signing); installed now so the version is locked early.
hyperliquid-python-sdk==0.24.0
